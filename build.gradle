/* Stockfish Java's build.gradle */

plugins {
    id 'application'
    id 'idea'
    id 'java'
}

group 'Niflheim'
version = new Version(major: 1, minor: 1, revision: 0, build: 2)
mainClassName = 'xyz.niflheim.ClientTester'

sourceCompatibility = 1.8
targetCompatibility = 1.8

def client_version = version.toString()
def stockfish_version = "10"

repositories {
    jcenter()
}

dependencies {
    compile 'ch.qos.logback:logback-classic:1.2.3'
}

class Version {
    String major, minor, revision, build

    String toString() {
        "${major}.${minor}.${revision}_${build}"
    }
}

tasks.withType(JavaCompile) {
    options.encoding = 'UTF-8'
}